services:
  postgres:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: reon
      POSTGRES_PASSWORD: reon
      POSTGRES_DB: reon
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
